2020-3-26：
1、header中增加了uid,name,dept,depts,roles五个参数，分别代表用户ID，用户姓名，用户所属部门，用户管辖部门数组，用户角色数组
2、增加了CurrentUser注解，可以加入到Controller参数中，自动注入当前用户对象（如果header中传入参数的话）
3、多表查询接口增加了默认值字段，可以为查询字段设置默认值，支持常量值和占位符，${uid},${dept}，目前还不支持全部操作符,
4、